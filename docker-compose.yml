version: '3.8'

services:
  mc_auth:
    build: .
    ports:
      - "3005:3005"
    environment:
      - JWT_SECRET=cestpourlasciencemerciderestercourtoi
      - REFRESH_TOKEN_SECRET=jesensquelesemmerdesvontcommenceraveccettehistoirederefreshtokentoutcapoursetaperundauprojet
      - PORT=3005
      - DB_NAME=kittydelivery
      - DB_ID_NAME=postgres
      - DB_ID_PASSWORD=admin
      - DB_HOST=postgres
      - DB_DIALECT=postgres
    networks:
      - shared-net

networks:
  shared-net:
    external: true
